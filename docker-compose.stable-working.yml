version: '3.8'

services:
  web-stable:
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "0.0.0.0:8080:8000"  # Puerto estable
    environment:
      - DEBUG=True
      - SECRET_KEY=django-insecure-stable-key
      - DATABASE_URL=postgres://motocenter:motocenter_stable@db-stable:5432/motocenter_stable
    depends_on:
      - db-stable
    restart: unless-stopped
    container_name: motocenter-web-stable

  db-stable:
    image: postgres:15-alpine
    volumes:
      - postgres_data_stable:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: motocenter_stable
      POSTGRES_USER: motocenter
      POSTGRES_PASSWORD: motocenter_stable
    ports:
      - "0.0.0.0:5433:5432"
    restart: unless-stopped
    container_name: motocenter-db-stable

  frontend-stable:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    command: npm run dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "0.0.0.0:3080:3000"  # Puerto estable
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=http://192.168.1.18:8080
    depends_on:
      - web-stable
    restart: unless-stopped
    container_name: motocenter-frontend-stable

volumes:
  postgres_data_stable:

networks:
  default:
    name: motocenter-stable-network